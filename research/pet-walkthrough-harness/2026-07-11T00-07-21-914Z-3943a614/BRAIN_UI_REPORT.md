# Packaged Brain and focus walkthrough

Run ID: `2026-07-11T00-07-21-914Z-3943a614`

Result: **PASS** for the packaged Brain matrix, private-thought lifecycle, silent decision boundary, and transparent-window focus cycle.

## Brain modes

Computer use exercised the actual Settings form and observed:

- Brain Off returned `BRAIN OFF` without an endpoint call.
- Local model connected to the fixture successfully.
- Cloud API connected through the same bounded HTTP adapter.
- Agent hook connected through the same bounded HTTP adapter.

The labels differ for the owner, while all enabled modes intentionally converge on the same inert-context, one-decision transport contract.

## Endpoint matrix

- Success returned `CONNECTED`.
- An unreachable endpoint returned the existing readable connection failure.
- A rejected fixture key returned `That brain endpoint rejected the API key.`
- A valid fixture key returned `CONNECTED`.
- A response delayed beyond the packaged `1,200 ms` test timeout returned `The brain endpoint timed out. Try again.`
- Non-JSON model content returned `That brain replied in an unreadable format.`

The fixture writes request time, route, body byte count, SHA-256, model, message count, and key-present/key-valid booleans. It never writes authorization values or prompt bodies.

## Private thought and silence

The saved local brain performed one real agent tick and returned the private thought `the shared tide is worth watching quietly` with `action: null`.

Computer use observed the thought bubble on the raft for five seconds and then confirmed it did not reappear. The persisted thought is `seen`. The receiver database still contains exactly one postcard, the previously received remote Waku observation, and zero postcards authored by the local Cypher. A private thought therefore did not create false public activity.

## Focus behavior

Computer use minimized and restored the device, moved focus to File Explorer, inspected the unfocused transparent region without a native title strip, and returned focus to an opaque interactive device. The compositor refresh now marks both minimize and blur as requiring a delayed invalidate, bounds pulse, hide/show, and focus after restoration.

## Artifacts

- `brain-fixture.json`: local fixture process and endpoint manifest.
- `brain-fixture-requests.jsonl`: redacted append-only request measurements.
- `brain-fixture-summary.json`: deterministic assertions for endpoint coverage, thought persistence, and public silence.
- `WAKU_UI_REPORT.md`: the remote postcard that supplied the thought run's bounded network context.

No API key, private key, prompt body, or plaintext wallet backup is present in these artifacts.
